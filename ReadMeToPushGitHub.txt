GYM AI ATTENDANCE SYSTEM — GITHUB UPLOAD GUIDE

STEP 1: OPEN TERMINAL
C:\xampp\htdocs\gym


STEP 2: CHECK STATUS
Run:
git status
 

STEP 3: ADD ALL FILES
Run:
git add .


STEP 4: COMMIT CHANGES
Run:
git commit -m "Text Here"


STEP 5: PUSH TO GITHUB
Run:
git push origin main


STEP 6: VERIFY UPLOAD
Go to your GitHub repository:
https://github.com/ValSalcedo/gym-ai-attendance-system

Refresh the page and check if files are updated.






IF FIRST TIME SETUP (ONLY ONCE):

If git is not initialized:
git init

Connect to your repo:
git remote add origin https://github.com/ValSalcedo/gym-ai-attendance-system.git

Then:
git branch -M main
git push -u origin main






OPTIONAL (GOOD PRACTICE):

Before pushing:
git pull origin main

